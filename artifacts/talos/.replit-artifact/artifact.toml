kind = "web"
previewPath = "/"
title = "TALOS System"
version = "1.0.0"
id = "artifacts/talos"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22420

[services.development]
run = "pnpm --filter @workspace/talos run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/talos", "run", "build" ]
publicDir = "artifacts/talos/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22420"
BASE_PATH = "/"
